name: dbt CI (PR checks)

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  dbt-ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dbt (BigQuery adapter)
        run: |
          python -m pip install --upgrade pip
          pip install dbt-bigquery

      - name: Create CI profiles.yml (dummy)
        run: |
          mkdir -p ~/.dbt
          cat > ~/.dbt/profiles.yml <<'PROFILES'
          people_analytics_dbt:
            target: ci
            outputs:
              ci:
                type: bigquery
                method: oauth
                project: dummy
                dataset: dummy
                location: US
                threads: 4
          PROFILES

      - name: dbt deps
        working-directory: domains/people_analytics
        run: dbt deps

      - name: dbt compile (CI)
        working-directory: domains/people_analytics
        run: dbt compile
